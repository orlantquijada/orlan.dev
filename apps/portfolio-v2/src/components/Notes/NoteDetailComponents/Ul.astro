---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'ul'>

const props = Astro.props
---

<ul {...props} class:list={['ul', props.class]}>
  <slot />
</ul>

<style>
  :global(.ul li) {
    @apply pl-8 relative;
  }

  :global(.ul li::before) {
    content: '— ';
    @apply text-gray11 absolute left-0 top-0;
  }
</style>
