---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"ul">;

const props = Astro.props;
---

<ul {...props} class:list={['ul', props.class]}>
  <slot />
</ul>

<style>
  :global(.ul li) {
    @apply relative pl-8;
  }

  :global(.ul li::before) {
    content: '— ';
    @apply absolute left-0 top-0 text-gray11;
  }
</style>
