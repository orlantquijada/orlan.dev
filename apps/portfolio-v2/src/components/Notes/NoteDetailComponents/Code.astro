---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"code">;
const props = Astro.props;
---

<code {...props} class:list={['rounded-md bg-gray7 p-1 text-xs',props.class]}>
	<slot/>
</code>
