---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"code">;
const { class: className, ...props } = Astro.props;
---

<code class:list={['rounded-md bg-gray7 p-1 text-xs',className]} {...props}><slot /></code>

